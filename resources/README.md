# Resources

Put the Ubuntu 24.04 cloud image (qcow2) here before running:

```
ubuntu-24.04-minimal-cloudimg-amd64-compressed.img
```

**Download**: [Ubuntu Cloud Images](https://cloud-images.ubuntu.com/minimal/releases/jammy/release/)

Default credentials after first boot: `ubuntu` / `123456`
